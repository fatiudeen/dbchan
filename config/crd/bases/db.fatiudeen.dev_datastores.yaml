---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: datastores.db.fatiudeen.dev
spec:
  group: db.fatiudeen.dev
  names:
    kind: Datastore
    listKind: DatastoreList
    plural: datastores
    singular: datastore
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Datastore is the Schema for the datastores API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DatastoreSpec defines the desired state of Datastore
            properties:
              datastoreType:
                description: DatastoreType specifies the type of database (mysql,
                  postgres, sqlserver)
                type: string
              host:
                description: Host is the database host address
                type: string
              instance:
                description: Instance is the SQL Server instance name (optional)
                type: string
              port:
                description: Port is the database port (defaults will be set based
                  on datastore type)
                format: int32
                type: integer
              secretRef:
                description: SecretRef references a secret containing only the database
                  password
                properties:
                  name:
                    description: Name is the name of the secret
                    type: string
                  passwordKey:
                    description: 'PasswordKey is the key in the secret containing
                      the password (default: "password")'
                    type: string
                required:
                - name
                type: object
              sslMode:
                description: 'SSLMode is the SSL mode for PostgreSQL connections (default:
                  "disable")'
                type: string
              username:
                description: Username is the database username
                type: string
            required:
            - datastoreType
            - host
            - secretRef
            - username
            type: object
          status:
            description: DatastoreStatus defines the observed state of Datastore
            properties:
              message:
                description: Message provides additional information about the current
                  state
                type: string
              phase:
                description: Phase represents the current phase of the datastore
                type: string
              ready:
                description: Ready indicates whether the datastore is ready for use
                type: boolean
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
