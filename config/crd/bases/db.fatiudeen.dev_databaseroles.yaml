---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: databaseroles.db.fatiudeen.dev
spec:
  group: db.fatiudeen.dev
  names:
    kind: DatabaseRole
    listKind: DatabaseRoleList
    plural: databaseroles
    singular: databaserole
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.created
      name: Created
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: DatabaseRole is the Schema for the databaseroles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DatabaseRoleSpec defines the desired state of DatabaseRole
            properties:
              databaseRef:
                description: DatabaseRef references a specific database (optional,
                  for database-specific roles)
                properties:
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              datastoreRef:
                description: DatastoreRef references the datastore where this role
                  will be created
                properties:
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              description:
                description: Description provides a human-readable description of
                  the role
                type: string
              isGlobal:
                description: IsGlobal indicates whether this is a global role (affects
                  all databases)
                type: boolean
              privileges:
                description: Privileges defines the database privileges for this role
                items:
                  type: string
                type: array
            required:
            - datastoreRef
            type: object
          status:
            description: DatabaseRoleStatus defines the observed state of DatabaseRole
            properties:
              created:
                description: Created indicates whether the role has been created in
                  the database
                type: boolean
              lastUpdated:
                description: LastUpdated indicates when the role was last updated
                format: date-time
                type: string
              message:
                description: Message provides additional information about the role
                  status
                type: string
              phase:
                description: Phase represents the current phase of the role
                type: string
              ready:
                description: Ready indicates whether the role is ready for use
                type: boolean
            required:
            - created
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
