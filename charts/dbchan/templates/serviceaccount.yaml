apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dbchan.fullname" . }}-controller-manager
  labels:
  {{- include "dbchan.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.controllerManager.serviceAccount.annotations | nindent 4 }}
