apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "dbchan.fullname" . }}-user-editor-role
  labels:
  {{- include "dbchan.labels" . | nindent 4 }}
rules:
- apiGroups:
  - db.fatiudeen.dev
  resources:
  - users
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - db.fatiudeen.dev
  resources:
  - users/status
  verbs:
  - get
